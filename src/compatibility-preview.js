/**
 * Reserved compatibility-preview seam.
 *
 * A later phase may add a public-API editor equivalent of a narrowly proven
 * render bridge. This file intentionally injects no styles and registers no
 * editor filters.
 */
